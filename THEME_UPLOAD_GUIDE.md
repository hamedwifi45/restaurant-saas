# دليل رفع وإدارة الثيمات (Theme Management)

## نظرة عامة
تمكين المدير من رفع ثيمات جديدة للنظام والتحقق من أنها تستوفي المتطلبات الأساسية المشابهة لـ `burger-theme`.

## المتطلبات الأساسية للثيم

يجب أن يحتوي أي ثيم على الهيكلية التالية:

```
theme-name/
├── theme.json              # ملف تعريف الثيم (مطلوب)
├── layout.blade.php        # ملف التخطيط الرئيسي (مطلوب)
├── preview.png             # صورة المعاينة (اختياري)
├── cart/                   # مجلد صفحات سلة التسوق
├── orders/                 # مجلد صفحات الطلبات
├── pages/                  # مجلد الصفحات الرئيسية
└── reviews/                # مجلد صفحات التقييمات
```

## هيكلية ملف theme.json

يجب أن يحتوي ملف `theme.json` على الحقول التالية:

```json
{
  "name": "اسم الثيم",
  "slug": "theme-slug",
  "author": "اسم المطور",
  "version": "1.0.0",
  "description": "وصف الثيم",
  "default_settings": {
    "primary_color": "#FF6B35",
    "secondary_color": "#FFFFFF",
    "background_color": "#1A1A1A",
    "font_family": "Tajawal",
    "border_radius": "0.5rem",
    "header_style": "sticky"
  },
  "allowed_variables": {
    "primary_color": {
      "type": "color",
      "label": "اللون الرئيسي",
      "default": "#FF6B35"
    },
    "font_family": {
      "type": "select",
      "label": "نوع الخط",
      "options": ["Tajawal", "Cairo", "Almarai"],
      "default": "Tajawal"
    }
  }
}
```

### الحقول المطلوبة في theme.json:

1. **name**: اسم الثيم (نص)
2. **slug**: رابط فريد للثيم (أحرف صغيرة، أرقام، وشرطات فقط)
3. **version**: إصدار الثيم بصيغة X.Y.Z (مثال: 1.0.0)
4. **default_settings**: كائن يحتوي على الإعدادات الافتراضية
   - يجب أن يحتوي على الأقل على: `primary_color`, `secondary_color`, `background_color`
5. **allowed_variables**: كائن يحتوي على المتغيرات المسموح بتعديلها
   - كل متغير يجب أن يحتوي على `type` و `label`

### أنواع المتغيرات المسموحة:

- `color`: لاختيار الألوان
- `select`: لاختيار من قائمة خيارات
- `text`: لنص عادي
- `number`: لأرقام

## خطوات التحقق (Validation)

عند رفع ثيم جديد، يقوم النظام بالتحقق من:

1. ✅ صحة ملف ZIP وعدم تلفه
2. ✅ وجود ملف `theme.json`
3. ✅ صحة صيغة JSON
4. ✅ وجود جميع الحقول المطلوبة
5. ✅ صحة تنسيق `slug` (أحرف صغيرة وأرقام وشرطات فقط)
6. ✅ صحة تنسيق `version` (X.Y.Z)
7. ✅ وجود الإعدادات الافتراضية المطلوبة
8. ✅ صحة هيكلية `allowed_variables`
9. ✅ وجود ملف `layout.blade.php`
10. ✅ عدم وجود ثيم بنفس الـ slug مسبقاً

## طريقة الاستخدام

### من لوحة التحكم:

1. انتقل إلى **السمات** من القائمة الجانبية
2. اضغط على زر **"رفع ثيم جديد"** في الأعلى
3. اختر ملف ZIP الذي يحتوي على الثيم
4. اضغط على **"رفع"**
5. سيقوم النظام بـ:
   - فك ضغط الملف
   - التحقق من استيفاء الشروط
   - نقل الملفات إلى المجلد الصحيح
   - إضافة سجل في قاعدة البيانات
   - حفظ صورة المعاينة إن وجدت

### بعد الرفع الناجح:

- سيكون الثيم في حالة **غير مفعل** افتراضياً
- يمكن للمدير تفعيله من جدول الثيمات
- يمكن تعيينه كثيم **افتراضي** للمطاعم الجديدة
- يمكن تعديله لتغيير الصورة أو الحالة

## إدارة الثيمات

### التفعيل/الإيقاف:
- اضغط على زر **"تفعيل"** لتفعيل الثيم
- اضغط على زر **"إيقاف"** لإيقافه (لا يمكن إيقاف الثيم الافتراضي)

### التعيين كثيم افتراضي:
- اضغط على **"جعل افتراضي"** لجعل الثيم هو الافتراضي لجميع المطاعم الجديدة
- يمكن وجود ثيم افتراضي واحد فقط

### الحذف:
- عند حذف ثيم، سيتم حذف جميع الملفات المرتبطة به
- لا يمكن حذف الثيم إذا كان مستخدماً من قبل مطاعم (يجب نقل المطاعم أولاً)

## مثال: إنشاء ثيم جديد

### 1. إنشاء مجلد الثيم:
```bash
my-restaurant-theme/
├── theme.json
├── layout.blade.php
├── preview.png
├── cart/
│   └── index.blade.php
├── orders/
│   └── history.blade.php
├── pages/
│   ├── home.blade.php
│   └── menu.blade.php
└── reviews/
    └── list.blade.php
```

### 2. إنشاء ملف theme.json:
```json
{
  "name": "مطعم حديث",
  "slug": "modern-restaurant",
  "author": "أحمد محمد",
  "version": "1.0.0",
  "description": "ثيم عصري للمطاعم مع تصميم بسيط وأنيق",
  "default_settings": {
    "primary_color": "#2ECC71",
    "secondary_color": "#ECF0F1",
    "background_color": "#FFFFFF",
    "font_family": "Cairo",
    "border_radius": "8px",
    "header_style": "transparent"
  },
  "allowed_variables": {
    "primary_color": {
      "type": "color",
      "label": "اللون الرئيسي",
      "default": "#2ECC71"
    },
    "secondary_color": {
      "type": "color",
      "label": "اللون الثانوي",
      "default": "#ECF0F1"
    },
    "background_color": {
      "type": "color",
      "label": "لون الخلفية",
      "default": "#FFFFFF"
    },
    "font_family": {
      "type": "select",
      "label": "نوع الخط",
      "options": ["Cairo", "Tajawal", "Almarai"],
      "default": "Cairo"
    }
  }
}
```

### 3. ضغط المجلد:
```bash
zip -r modern-restaurant.zip my-restaurant-theme/
```

### 4. رفع الملف عبر لوحة التحكم

## المجلدات المستخدمة

- **مجلد الثيمات**: `resources/views/themes/`
- **صور المعاينة**: `storage/app/public/themes/previews/`
- **الملفات المؤقتة**: `storage/app/temp/themes/`

## الأخطاء الشائعة

| الخطأ | السبب | الحل |
|-------|-------|-----|
| "ملف theme.json غير موجود" | الملف غير موجود أو في مكان خاطئ | تأكد من وجود theme.json في الجذر أو مجلد فرعي مباشر |
| "الرابط المختصر (slug) يجب أن يحتوي على أحرف صغيرة" | استخدام أحرف كبيرة أو رموز | استخدم أحرف صغيرة وأرقام وشرطات فقط |
| "إصدار الثيم يجب أن يكون بصيغة X.Y.Z" | صيغة الإصدار خاطئة | استخدم صيغة مثل 1.0.0 أو 2.1.3 |
| "ثيم بنفس الرابط المختصر موجود مسبقاً" | التكرار | اختر slug فريد مختلف |
| "الملف 'layout.blade.php' مطلوب" | الملف مفقود | أضف ملف layout.blade.php للثيم |

## ملاحظات هامة

1. **الأمان**: يتم رفع الثيمات في مجلد مؤقت ثم التحقق منها قبل النقل النهائي
2. **التنظيف التلقائي**: يتم حذف الملفات المؤقتة تلقائياً بعد المعالجة
3. **النسخ الاحتياطي**: يُنصح بأخذ نسخة احتياطية قبل رفع ثيمات جديدة
4. **الاختبار**: اختبر الثيم جيداً قبل رفعه للإنتاج
5. **التوافق**: تأكد من توافق الثيم مع إصدار النظام

## الدعم الفني

في حال واجهت أي مشاكل، راجع سجلات النظام في:
- `storage/logs/laravel.log`

أو تحقق من رسائل الخطأ في لوحة التحكم.
